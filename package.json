{
  "name": "mozaiks-core",
  "version": "1.0.0",
  "packageManager": "npm@10.9.3",
  "private": true,
  "license": "MIT",
  "description": "Mozaiks Core - Open source runtime, SDK, and AI engine",
  "workspaces": [
    "runtime/packages/*",
    "runtime/ai/packages/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
