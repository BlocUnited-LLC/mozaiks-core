# Mozaiks Keycloak theme (login)
# Parent keeps Keycloak templates; we override styling via CSS.
parent=keycloak.v2

# Load our CSS after Keycloak's base styles.
styles=css/mozaiks.v12.css

# Ensure browser caches are reasonable (Keycloak still controls overall caching)
locales=en
